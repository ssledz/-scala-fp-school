package fpdesign

/**
 *  - applicative type class
 *  - validate vs either
 *  - laws
 *    - identity law     x.map(a => a) == x
 *    - composition law  x.map(f andThen g) == x.map(f).map(g)
 *    - homomorphism
 *    - interchange
 */
object applicative {

}
